import React from 'react';
import {Button} from "@mui/material";

const SubmitButton = (prop) => {
    function handles
    return (
        <div>
            <Button onClick={handleFunciton}></>
        </div>
    );
};

export default SubmitButton;